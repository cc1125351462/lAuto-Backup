module.exports = {

    //mail config
    mail: {},

    //file path to backup
    file: '',

    //if set true will compress a zip file to store
    compress: true,

    //time interval to backup file ( default: 1 hour )
    time: 1000 * 60 * 60
};